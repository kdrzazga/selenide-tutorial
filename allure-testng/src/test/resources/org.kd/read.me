To generate allure-report:
1. Make sure allure is installed and added to OS PATH
2. Run test suite testng.xml
3. When allure-results director is created, run "allure serve"
4. Allure report will be opened in default web-browser.
